# ============================================================
# 服务配置 (Service Configuration)
# ============================================================
# 定义应用服务的网络端口设置
# ============================================================

# 应用服务端口：FastAPI 服务器监听的端口
# 访问地址：http://localhost:{app_port}
app_port: ${APP_PORT:-9023}
